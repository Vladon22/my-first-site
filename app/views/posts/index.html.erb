<h1>Всі наші пости</h1>
<% @post.each do |post| %>
   <div class="well">
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to "Ще", post_path(post), :class => 'btn btn-outline-primary' %>
<% end %>